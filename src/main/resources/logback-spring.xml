<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender with MDC values including trace info -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [traceId=%X{traceId},spanId=%X{spanId},component=%X{component},requestId=%X{requestId},userId=%X{userId}] - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- File appender with MDC values including trace info -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover -->
            <fileNamePattern>logs/application.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- Keep 30 days worth of history -->
            <maxHistory>30</maxHistory>
            <!-- Total size cap -->
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [traceId=%X{traceId},spanId=%X{spanId},component=%X{component},requestId=%X{requestId},userId=%X{userId}] - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Set logging levels for different packages -->
    <logger name="com.edge.template" level="DEBUG" />
    
    <!-- Spring Framework -->
    <logger name="org.springframework" level="INFO" />
    <logger name="org.hibernate" level="WARN" />
    
    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>